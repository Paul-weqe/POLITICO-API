language: python

dist: xenial

python:
  - "3.6"

install:
  - pip install -r requirements.txt
  - pip install python-coveralls
  - pip install pytest

script: 
  - bash build.sh

after-success:
  - coveralls


